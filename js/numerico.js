function numerico() {

                      if ( isNaN(document.formulario.pwd.value) )
                         {
                          alert("Digite somente caracteres numéricos!") ;
                          document.formulario.pwd.value = "";
                          return false;
                         }
                    }

function numericoN() {

                      if ( isNaN(document.formulario.new_pwd.value) )
                         {
                          alert("Digite somente caracteres numéricos!") ;
                          document.formulario.new_pwd.value = "";
                          return false;
                         }
                    }

