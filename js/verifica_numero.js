function numerico();
{
if ( isNaN(document.formulario.pwd.value) )
  {

        alert("Você não digitou un número !") ;
        document.araxa.v1.value = "";
        return false;
   }
   return true;
}